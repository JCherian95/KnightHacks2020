#ifndef UTIL_H
#define UTIL_H


class Util
{
    public:
        Util();
        virtual ~Util();
        Util(const Util& other);
        Util& operator=(const Util& other);

    protected:

    private:
};

#endif // UTIL_H
